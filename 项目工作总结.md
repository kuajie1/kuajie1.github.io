# 冰雪奇缘百科全书 · 项目工作总结

> 生成时间：2026-08-18
> 项目路径：`D:\学习资料\frozen\frozen-encyclopedia-vitepress`
> 线上地址（部署后）：https://kuajie1.github.io/
> 本地预览：http://localhost:8199/

---

## 一、我们要做什么（项目目标）

做一个关于迪士尼《冰雪奇缘》系列（Frozen / Frozen II 及相关短片、小说）的**中文图文百科网站**，要求：

- **技术形态**：基于 VitePress（Vue 3 静态站点生成器）构建，最终发布到 GitHub Pages，任何人都能通过网址直接访问。
- **内容范围**：覆盖 10 个主题卷——
  | 卷 | 主题 | 卷 | 主题 |
  |---|---|---|---|
  | vol1 | 角色 | vol6 | 时间线 |
  | vol2 | 世界观 | vol7 | 制作幕后 |
  | vol3 | 魔法 | vol8 | 文化影响 |
  | vol4 | 剧情 | vol9 | 歌曲 |
  | vol5 | 主题 | vol10 | 小说 |
- **核心特色**：不只是静态文字，而是带交互的"百科"——小说阅读器、角色信息卡、谱系图、关系图、术语悬停解释、图片灯箱、阅读进度条等。
- **设计要求**：极光冰晶视觉语言（玻璃拟态 + 衬线标题 + 雪花点缀），整体风格统一、精致。
- **图片策略**：用户明确要求**图片不压缩**（保持清晰），因此全部 749 MB / 1913 张原图直出，通过 Git LFS 管理。

---

## 二、我们已经做了什么（工作阶段）

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1. 优化启动 | 恢复迁移时丢失的 4 张 Mermaid 图、3 张对比表；确认图片零压缩 | ✅ 完成 |
| 2. 加深薄页 + 全局交互 | 扩充 vol3（魔法）/vol5（主题）薄页；新增 `SiteEnhance`（灯箱 + 阅读进度条） | ✅ 完成 |
| 3. 小说阅读器 Pro | 重写 `NovelReader`，达到"小说阅读软件"级体验 | ✅ 完成 |
| 4. 五项修复 | 修复：待译误显 / 双栏上下改左右 / 无法翻页 / 界面不精致 / 小标题无区分 | ✅ 完成 |
| 5. 收尾补全 | 还原 vol4/vol8 对比表、`SpoilerBox` 剧透折叠、术语悬停、小说小节折叠、Mermaid 懒加载瘦身 | ✅ 完成 |
| 6. 部署上线 | git 初始化 + LFS + 提交 + GitHub Actions 自动部署 | ✅ 代码已推，部署待重启 |
| 7. Fandom 借鉴 v1 | 新增 `CharacterInfobox` + `CharNavbox`，套用到安娜/艾莎页 | ✅ 已推送（f15f2f9） |
| 8. 排版重设计 | 推翻 v1 丑陋样式，新建 `CharProfile`（极光+玻璃拟态）重做角色页 | 🟡 已改文件，未提交 |

**累计成果**：125 个 Markdown 内容页、13 个 Vue 交互组件、1913 张 LFS 图片（749 MB）。

---

## 三、现在是什么情况（当前状态）

### 代码与版本
- 本地仓库有 2 个提交，且都已推到 GitHub：
  - `cfe93f6` 整站初始化（源码 + 图片走 LFS）
  - `f15f2f9` Fandom 借鉴 v1（`CharacterInfobox` + `CharNavbox`）
- **最新重设计（阶段 8）尚未提交**，当前工作区改动：
  - 新增 `src/.vitepress/theme/components/CharProfile.vue`
  - 修改 `CharNavbox.vue`、`index.js`、`anna.md`、`elsa.md`

### 部署
- GitHub Actions 已成功触发：**构建（build）通过**（约 2 分 31 秒，证明代码与图片在 CI 上都能正常构建）。
- **部署（deploy）步骤失败**，原因是 GitHub Pages 服务端返回 `503`（GitHub 自身瞬时抖动，非代码或配置问题），只需重跑部署即可。
- 仓库 Pages 源已正确设为 "GitHub Actions"。

### 本地预览
- `http://localhost:8199/` 正常运行（HTTP 200），可随时查看当前效果。

### 已知问题（重要）
- **生产构建偶发卡死**：构建进程停在 "building client + server bundles" 不动。根因是项目里的 **safe-delete 守卫脚本**（`genie-safe-delete.cjs`，它会拦截 `rm`、强制用 `mv` 清理 dist）在多个 VitePress 进程同时出现时会互相锁死。解决办法：构建前确保只跑一个构建进程、关闭 dev 服务器、清理 `src/.vitepress/cache` 后再 build。
- 旧 `_old_dist_*` 临时目录可能在守卫触发时累积，需手动 `mv` 到备份目录清理（受删除守卫保护，不能直接 `rm`）。

---

## 四、关键功能是怎么实现的（实现方式）

### 整体架构
- **VitePress v1.6.4 + Vue 3**，`root=src`，构建产物输出到 `src/.vitepress/dist`。
- 所有自定义组件在 `src/.vitepress/theme/index.js` 里用 `app.component()` **全局注册**，Markdown 中可直接以标签形式调用（如 `<NovelReader ... />`）。
- 全站视觉变量集中在 `src/.vitepress/theme/custom.css`（极光冰晶配色 `--fz-aurora-*`、玻璃拟态、`.dark` 暗色覆盖、衬线标题、雪花动画）。

### 1. 小说阅读器 `NovelReader.vue`（客户端渲染）
- 在浏览器端渲染（非构建期），支持：目录抽屉、书签 + `localStorage` 续读、4 套排版主题（纸张/羊皮纸/暗夜/极简，用 CSS 变量切换）、字号/行距滑杆、章节进度条、左右翻页、书内搜索、双栏 grid 左右并排、右下角常驻"下一章"浮窗、章节末卡片、本章小节折叠。
- 小节通过 `parseSubsections` 解析、`jumpToSub` 跳转、`onContentClick` 折叠交互。

### 2. 角色信息卡 `CharProfile.vue`（阶段 8 新设计）
- **极光人物主视觉**：衬线渐变角色名 + 角色定位副标题。
- **玻璃拟态资料卡**：极光渐变边框头像 + 事实网格（英文名/身份/登场作品/配音分段/按作品年龄/亲属/盟友/敌人/能力）；长字段自动占整行。
- 与全站玻璃拟态/极光语言统一，含 `.dark` 暗色适配。取代阶段 7 那个突兀的白色 Fandom 方盒。

### 3. 全局增强 `SiteEnhance.vue`（注入到 `layout-top`）
- **灯箱**：点击图片放大查看。
- **阅读进度条**：页面顶部随滚动显示的极光进度条。
- **术语悬停**：导入 `src/terms/terms-data.js`（39 条术语词库），遍历正文给术语包 `<span class="term-tip">`，悬停弹出 `.term-pop` 解释卡；自动跳过 H1–H3、链接、代码、按钮等元素。

### 4. 剧透折叠 `SpoilerBox.vue`
- 默认折叠，带 ⚠ 预警头，明暗自适应；用于 vol10 小说各册"内容简介（剧情梗概）"等剧透内容。

### 5. Mermaid 关系/谱系图 `MermaidDiagram.vue`
- 引入 `mermaid@10`，改为函数内 `await import('mermaid')` **动态导入**，把图表拆成独立 chunk 懒加载，不拖慢首屏。
- 另有 `LineageTree`（家族谱系）、`ReleaseTimeline`（上映时间线）、`Timeline` 等组件。

### 6. 小说翻译回填 `scripts/fix_top_zh.py`
- 修复 junior / junior2 小说"已翻译 100% 却显示待译"的问题：回填每章 `chapter.zh` 顶层字段（句子拼接为 HTML）。

### 7. 部署流水线 `.github/workflows/deploy.yml`
- 官方 GitHub Actions 方式：`checkout` 开 `lfs: true`（构建时把 LFS 指针还原成真实图片）→ Node 22 + `npm ci` → `vitepress build` → `configure-pages` → `upload-pages-artifact` → `deploy-pages`。
- 图片通过 **Git LFS（3.7.1）** 管理 749 MB / 1913 张，避免污染 git 文本历史。

### 8. 视觉与交互统一（`custom.css` + `SnowOverlay.vue`）
- 极光配色变量、玻璃拟态卡片、衬线大标题、雪花飘落覆盖层（`SnowOverlay` 注入 `layout-bottom`），全站统一的"冰雪"氛围。

---

## 五、下一步建议（待办）

1. **提交并推送阶段 8 重设计**：`git add` 改动 → commit → `git push -u origin main`（远程已含前两个提交，且 `workflow` 权限已补好）。
2. **重跑部署**：等 GitHub Pages 503 恢复后，在 Actions 里重跑失败的 deploy 步骤，公开网址即可访问。
3. **推广角色页新排版**：把 `CharProfile` 套用到克里斯托夫、雪宝、汉斯、伊杜娜与阿格纳尔等其余角色页。
4. **清理构建守卫卡死**：构建前确保单进程 + 关 dev + 清 cache；手动 `mv` 清理累积的 `_old_dist_*`。
5. **可选增强**：移动端适配、搜索分面、暗色细节打磨、术语库扩充、图片按需懒加载（提速，需在不压缩前提下另议）。

---

## 六、一句话结论

网站功能与内容已全部做完并跑通本地预览；代码已推到 GitHub，只差"重跑一次部署"即可公开访问。当前正在进行的是**角色页排版重设计**（阶段 8），文件已改好待提交。构建进程偶发卡死是本地 safe-delete 守卫的进程锁问题，不影响源文件，按上面的清理步骤即可规避。
